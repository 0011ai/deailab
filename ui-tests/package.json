{
  "name": "bacalhau_lab-ui-tests",
  "version": "1.0.0",
  "description": "JupyterLab bacalhau_lab Integration Tests",
  "private": true,
  "scripts": {
    "start": "jupyter lab --config jupyter_server_test_config.py",
    "test": "yarn playwright test",
    "test:debug": "PWDEBUG=1 yarn playwright test",
    "test:update": "yarn playwright test --update-snapshots"
  },
  "devDependencies": {
    "@jupyterlab/galata": "^5.0.0",
    "@playwright/test": "^1.32.0"
  }
}
